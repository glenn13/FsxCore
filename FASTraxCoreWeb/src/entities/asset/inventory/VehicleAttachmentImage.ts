import {Vehicle} from './Vehicle';
import {EntityAttachmentImage} from '../../global/EntityAttachmentImage';

export default interface VehicleAttachmentImage extends EntityAttachmentImage<Vehicle> {}
